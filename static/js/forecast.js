(() => {
  const summary=document.getElementById("forecast-summary"),discussion=document.getElementById("forecast-discussion"),issued=document.getElementById("forecast-issued"),satellite=document.getElementById("goes-image"),ad=document.getElementById("weather-ad");
  const track=event=>fetch("/api/forecast/ad-event",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({event}),keepalive:true}).catch(()=>{});
  track("impression"); ad.addEventListener("click",()=>track("click"));
  function speak(el,button){if(!("speechSynthesis" in window)){button.textContent="Speech unavailable";return}speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(el.textContent);u.rate=.96;button.textContent="■ Reading";u.onend=()=>button.textContent="▶ Read aloud";speechSynthesis.speak(u)}
  document.getElementById("read-forecast").addEventListener("click",e=>speak(summary,e.currentTarget));document.getElementById("read-discussion").addEventListener("click",e=>speak(discussion,e.currentTarget));
  async function loadForecast(){try{const r=await fetch("/api/forecast"),d=await r.json();if(!r.ok)throw new Error(d.error);const p=d.period;summary.textContent=`Forecast issued every 6 hours. ${p.name}. Temperature: ${p.temperature}°${p.temperatureUnit}. Wind: ${p.windSpeed}. Conditions: ${p.detailedForecast}`;issued.textContent="Last Updated: "+new Date().toLocaleString()}catch(e){summary.textContent="Forecast unavailable."}}
  function renderComments(data){document.getElementById("discussion-day").textContent=`Discussion for ${data.day} · refreshes daily at midnight Mountain Time`;discussion.replaceChildren();if(!data.comments.length){const p=document.createElement("p");p.textContent="No comments yet. Start today’s weather conversation.";discussion.append(p);return}data.comments.forEach(c=>{const article=document.createElement("article");article.className="comment";const header=document.createElement("header"),strong=document.createElement("strong"),time=document.createElement("time"),body=document.createElement("p");strong.textContent=c.display_name;time.textContent=new Date(c.created_at).toLocaleTimeString([], {hour:"numeric",minute:"2-digit"});body.textContent=c.comment;header.append(strong,time);article.append(header,body);discussion.append(article)})}
  async function loadComments(){const r=await fetch("/api/forecast/discussion"),d=await r.json();if(r.ok)renderComments(d)}
  document.getElementById("comment-form").addEventListener("submit",async e=>{e.preventDefault();const status=document.getElementById("comment-status");status.textContent="Posting…";const r=await fetch("/api/forecast/discussion",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:document.getElementById("comment-name").value,comment:document.getElementById("comment-text").value,website:document.getElementById("comment-website").value})});const d=await r.json();if(!r.ok){status.textContent=d.error||"Could not post.";return}document.getElementById("comment-text").value="";status.textContent="Posted.";renderComments(d)});
  loadForecast();loadComments();setInterval(loadForecast,21600000);setInterval(loadComments,60000);setInterval(()=>satellite.src="https://cdn.star.nesdis.noaa.gov/GOES19/ABI/CONUS/GEOCOLOR/1250x750.jpg?t="+Date.now(),300000);
})();
